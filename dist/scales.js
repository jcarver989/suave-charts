// Generated by CoffeeScript 1.10.0
var Scales,
  bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

Scales = (function() {
  Scales.fromString = function(x, y) {
    return new Scales(Scales.selectScale(x), Scales.selectScale(y));
  };

  Scales.selectScale = function(param) {
    switch (param) {
      case "linear":
        return d3.scale.linear();
      case "log":
        return d3.scale.log();
      case "time":
        return d3.time.scale();
      case "ordinal":
        return d3.scale.ordinal();
    }
  };

  function Scales(x1, y1) {
    this.x = x1;
    this.y = y1;
    this.setRanges = bind(this.setRanges, this);
    this.setDomains = bind(this.setDomains, this);
    this.scaleY = bind(this.scaleY, this);
    this.scaleX = bind(this.scaleX, this);
  }

  Scales.prototype.scaleX = function(pair) {
    return this.x(pair[0]);
  };

  Scales.prototype.scaleY = function(pair) {
    return this.y(pair[1]);
  };

  Scales.prototype.setDomains = function(points) {
    this.x.domain(d3.extent(points, function(d) {
      return d[0];
    }));
    return this.y.domain([
      0, d3.max(points, function(d) {
        return d[1];
      })
    ]);
  };

  Scales.prototype.setRanges = function(width, height) {
    this.x.range([0, width]);
    return this.y.range([height, 0]);
  };

  return Scales;

})();
