// Generated by CoffeeScript 1.7.1
var ColorManager, colors, defaultOptions;

colors = ["#1ABC9C", "#2ECC71", "#3498DB", "#9B59B6", "#34495E"];

ColorManager = (function() {
  function ColorManager() {
    this.colors = colors;
    this.assignedColors = {};
    this.current = 0;
  }

  ColorManager.prototype.getOrSet = function(key) {
    var _base;
    (_base = this.assignedColors)[key] || (_base[key] = this.nextColor());
    return this.assignedColors[key];
  };

  ColorManager.prototype.set = function(key, color) {
    return this.assignedColors[key] = color;
  };

  ColorManager.prototype.nextColor = function() {
    var index;
    index = this.current % this.colors.length;
    this.current += 1;
    return this.colors[index];
  };

  return ColorManager;

})();

defaultOptions = {
  xScale: d3.scale.linear(),
  yScale: d3.scale.linear()
};
