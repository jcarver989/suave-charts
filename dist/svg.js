// Generated by CoffeeScript 1.10.0
var Svg;

Svg = (function() {
  function Svg(selector, margin) {
    this.domElement = document.querySelector(selector);
    this.container = d3.select(this.domElement).append("svg").attr('preserveAspectRatio', 'xMinYMin');
    this.chart = this.container.append("g").attr("transform", "translate(" + margin.left + "," + margin.top + ")");
  }

  Svg.prototype.getSize = function() {
    return [this.domElement.offsetWidth, this.domElement.offsetHeight];
  };

  Svg.prototype.setSize = function(width, height) {
    return this.container.attr("width", width).attr("height", height);
  };

  return Svg;

})();
